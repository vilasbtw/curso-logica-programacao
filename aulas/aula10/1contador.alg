algoritmo "contador"
var

contador: inteiro

inicio

      contador <- 1
escrevaL("C O N T A N D O . . .")
      
    repita
          escrevaL(contador)
          contador <- contador + 1
    ate (contador > 10)
      
fimalgoritmo