algoritmo "contar10ate0"
var
contador: inteiro

inicio

contador <- 10

      enquanto (contador >= 0) faca
               escrevaL(contador)
               contador <- contador - 1
      fimEnquanto

escreva("Terminei de contar xD")

fimalgoritmo